# 地理信息管理系统

基于 Vite + Vue3 构建的地理信息管理系统原型，包含登录、仪表盘以及数据导入、地图浏览、数据查询、分析与导出等模块。主界面采用 Flex 布局，内置弹窗、进度条、提示信息与图表展示。

## 使用方式

```bash
npm install
npm run dev
```

如需构建发布版本：

```bash
npm run build
```

> 若安装依赖时受网络限制，可在 `package.json` 中更换可用的 npm registry。
